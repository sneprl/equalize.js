/**
 * @preserve
 * equalize.js
 * Dual MIT & GPL license
 *
 * Repo: https://github.com/Etiqa/equalize.js.git
 *
 * The jQuery plugin for equalizing the height or width of elements.
 *
 * Equalize will accept any of the jQuery Dimension methods:
 *   height, outerHeight, innerHeight,
 *   width, outerWidth, innerWidth.
 *
 * EXAMPLE
 * $('.parent').equalize(); // defaults to 'height'
 * $('.parent').equalize('width'); // equalize the widths
 *
 * ADVANCED EXAMPLES
 * Get the minimum max dimension by removing the existing height/width
 * $('.parent').equalize({reset: true}); // equalize height by default, remove existing height, then determin max
 * $('.parent').equalize({equalize: 'width', reset: true}); // equalize width, remove existing width, then determin max
 * $('.parent').equalize({checkPageResize: true}); // equalize on resize
 *
 * MULTI-ELEMENT EXAMPLE
 * Equalize elements
 * $('.elements').equalize();
 *
 * Equalize internal child elements
 * From @larsbo : http://jsfiddle.net/4QTNP/3/
 * $('.parent').equalize({children: 'p'}); // equalize height of paragraphs within .parent
 */
(function(a){a.fn.equalize=function(b){var c=this,d=false,e=false,f=false,g,h,i;function j(b){var c=0;b.css(h,"auto");b.each(function(){var b=a(this),d;if(e){b.css(h,"")}d=b[g]();if(d>c){c=d}});b.css(h,c+"px")}if(a.isPlainObject(b)){g=b.equalize||"height";d=b.children||false;e=b.reset||false;f=b.checkPageResize||false}else{g=b||"height"}if(!a.isFunction(a.fn[g])){return false}h=g.indexOf("height")!==-1?"height":"width";if(c.toArray().length>1){i=c}else{return c.each(function(){i=d?a(this).find(d):a(this).children()})}j(i);if(f){function k(){j(i)}a(window).resize(k)}return this}})(jQuery);